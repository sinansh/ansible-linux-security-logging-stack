---
# Ansible collections used by the roles:
#   ansible.posix      -> acl, seboolean
#   community.general  -> ini_file (audisp plugin toggle)
# Install with:  ansible-galaxy collection install -r requirements.yml
collections:
  - name: ansible.posix
  - name: community.general
